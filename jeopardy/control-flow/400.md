# Control Flow for 400

The following loop runs this many times:

```java
String s = "jeopardy";
for (int i = 0; i < s.length(); i++) {
  System.out.print(s.charAt(i));
}
```

{% include answer.html answer="8" %}
